<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Anikin.Views.VideoPlayerView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:berry="https://github.com/jerry08/Berry.Maui"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewModels="clr-namespace:Anikin.ViewModels"
    xmlns:views="clr-namespace:Anikin.Views"
    x:Name="this"
    x:DataType="viewModels:VideoPlayerViewModel"
    Shell.TabBarIsVisible="False">
    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
            <toolkit:IsStringNotNullOrWhiteSpaceConverter x:Key="IsStringNotNullOrWhiteSpaceConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid HorizontalOptions="Fill" VerticalOptions="Fill">
        <Grid
            BackgroundColor="{StaticResource Black}"
            HorizontalOptions="Fill"
            IsVisible="{Binding StartedPlaying, Converter={StaticResource InvertedBoolConverter}}"
            VerticalOptions="Fill"
            ZIndex="1000">
            <ActivityIndicator
                HeightRequest="{OnPlatform Default=60,
                                           WinUI=100}"
                HorizontalOptions="Center"
                IsRunning="{Binding StartedPlaying, Converter={StaticResource InvertedBoolConverter}}"
                VerticalOptions="Center"
                WidthRequest="{OnPlatform Default=60,
                                          WinUI=100}"
                Color="White" />
        </Grid>
        <berry:MediaElement
            x:Name="videoPlayer"
            HorizontalOptions="Fill"
            IsVisible="{Binding StartedPlaying}"
            ShouldAutoPlay="True"
            ShouldShowPlaybackControls="True"
            Source="{Binding Source}"
            VerticalOptions="Fill">
            <berry:MediaElement.Behaviors>
                <toolkit:EventToCommandBehavior
                    Command="{Binding BindingContext.LoadedCommand, Source={x:Reference this}}"
                    CommandParameter="{x:Reference videoPlayer}"
                    EventName="Loaded" />
                <toolkit:EventToCommandBehavior
                    Command="{Binding BindingContext.StateChangedCommand, Source={x:Reference this}}"
                    CommandParameter="{Binding CurrentState, Source={x:Reference videoPlayer}}"
                    EventName="StateChanged" />
                <toolkit:EventToCommandBehavior Command="{Binding BindingContext.UnloadedCommand, Source={x:Reference this}}" EventName="Unloaded" />
            </berry:MediaElement.Behaviors>
        </berry:MediaElement>
        <Border
            x:Name="subtitlesContainer"
            Margin="{OnPlatform Default='0,10',
                                WinUI='0,50'}"
            Padding="{OnPlatform '10,5',
                                 WinUI='20,10'}"
            BackgroundColor="#38000000"
            HorizontalOptions="Center"
            IsVisible="{Binding CurrentSubtitle, Converter={StaticResource IsStringNotNullOrWhiteSpaceConverter}}"
            StrokeThickness="0"
            VerticalOptions="End">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="8" />
            </Border.StrokeShape>
            <Grid>
                <Label
                    CharacterSpacing="0.1"
                    FontFamily="SoraBold"
                    FontSize="20"
                    HorizontalOptions="Center"
                    HorizontalTextAlignment="Center"
                    Opacity="0.95"
                    Text="{Binding CurrentSubtitle}"
                    TextColor="White" />
            </Grid>
        </Border>
    </Grid>
</ContentPage>
